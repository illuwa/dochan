## 변경 사항


## 테스트
- [ ] `uv run --locked --extra dev ruff check dochan scripts tests` 통과
- [ ] `uv run --locked --extra dev python -m pytest tests/` 통과
- [ ] (maintainer) 설치된 `secaudit`로 tracked-file 보안 게이트 통과
- [ ] 실제 HWP/HWPX 파일로 검증

## 관련 Issue
